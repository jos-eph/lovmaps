I would like you to draft up a markdown file that contains a plan for coding by an agentic coding agent. The objective of the file is to convert an existing script which generates PM tiles locally from open street map data, to a dynamic CI/CD flow that uploads dated or versioned builds of pmtiles and pbf files to GitHub Releases. A release to GitHub  triggers once a day, based on the script in the main branch, and also whenever push_release is set to true in the GitHub yaml . The plan should include security measures like branch protection, and recommendations for human decision on whatever other security protections are appropriate for the repo. Once I make decisions on the recommendations, the resulting markdown file should be sufficient to hand it over to a coding agent to set up my repository for the flow.  I intend to farm the work out to Claude Code with Opus 4.7, but I may have some parts completed by aider with qwen3-coder-next with a 64,000 token context window. Accordingly, please provide high quality prompts. Also, include instructions for a coding agent to write a detailed summary of remaining work and stop if the user is approaching 88% of the token limit for the session.  the base name of the files will be philly_commute_region, plus a date based on seconds-minutes-hours-day-month of generation. Each PBF or PMTILES file, upon completion, will be uploaded to github releases along with a file <<base_name>><<date>>.sha, which contains the sha256 hash of the file.  Additionally, each new release will overwrite philly_commute_region_current.pmtiles and its accompanying SHA, so that anyone who permanently links to that URL will always receive the latest file. 
